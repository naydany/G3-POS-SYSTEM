

const searchCate = document.getElementById('itme_cate');
const nameCategoryItem = document.querySelectorAll('tr');
const btn = document.querySelector('.btn');

btn.addEventListener('click', function () {
    // const search_item = searchCate.value.trim().toLowerCase();
    console.log(2)
    // nameCategory.forEach(function (category) {
    //     const name = category.children[1].textContent.toLowerCase();
    //     console.log(name);

    // })
})


